# Serve static files from /var/app/current/public under the URL path /static/
location /static/ {
    alias /var/app/current/public/;
    autoindex off;
    access_log off;
    try_files $uri $uri/ =404;
}

# If you want root to serve index.html instead of proxying to the app, uncomment:
#location = / {
#    try_files /public/index.html =404;
#}
