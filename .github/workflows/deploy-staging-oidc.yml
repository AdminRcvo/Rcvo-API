name: Déploiement STAGING via OIDC (Elastic Beanstalk)

on:
  push:
    branches: [ main ]         # adapte si besoin
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

# Évite les déploiements concurrents qui se marchent dessus
concurrency:
  group: eb-staging-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: eu-west-3
  APP_NAME: Rcvo-backend
  ENV_NAME: Rcvo-Backend-staging

jobs:
  deployer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::409818814260:role/GithubOIDCRcvoDeploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Vérifier l’identité AWS
        run: aws sts get-caller-identity --region "${AWS_REGION}"

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Installer dépendances (lockfile si présent)
        shell: bash
        run: |
          set -euo pipefail
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build (facultatif)
        run: npm run build --if-present

      - name: Pack ZIP à déployer
        id: pack
        shell: bash
        run: |
          set -euo pipefail
          TS="$(date +%Y%m%d_%H%M%S)"
          SHA="${GITHUB_SHA::7}"
          ZIP="app-${SHA}-${TS}.zip"
          echo "zip=$ZIP" >> "$GITHUB_OUTPUT"
          # Exclure dossier git, node_modules et l’archive elle-même
          zip -r "$ZIP" . -x ".git/*" "node_modules/*" "$ZIP"

      - name: Déterminer le bucket EB (CreateStorageLocation idempotent)
        id: bucket
        shell: bash
        run: |
          set -euo pipefail
          BUCKET="$(aws elasticbeanstalk create-storage-location \
            --region "${AWS_REGION}" \
            --query S3Bucket --output text)"
          echo "bucket=$BUCKET" >> "$GITHUB_OUTPUT"

      - name: Upload vers S3
        run: |
          aws s3 cp "./${{ steps.pack.outputs.zip }}" \
            "s3://${{ steps.bucket.outputs.bucket }}/${{ steps.pack.outputs.zip }}" \
            --region "${AWS_REGION}"

      - name: Créer/enregistrer la version d’appli (idempotent)
        id: appver
        shell: bash
        run: |
          set -euo pipefail
          LABEL="${{ steps.pack.outputs.zip }}"
          aws elasticbeanstalk create-application-version \
            --region "${AWS_REGION}" \
            --application-name "${APP_NAME}" \
            --version-label "$LABEL" \
            --source-bundle S3Bucket="${{ steps.bucket.outputs.bucket }}",S3Key="${{ steps.pack.outputs.zip }}" \
          || echo "Version déjà existante, on continue."
          echo "label=$LABEL" >> "$GITHUB_OUTPUT"

      # S'assurer que l'env est bien 'Ready/Green' AVANT update
      - name: Attendre READY/GREEN (pré-déploiement)
        shell: bash
        run: |
          set -euo pipefail
          END=$((SECONDS + 10*60))
          echo "⏳ Pré-déploiement : on attend ${ENV_NAME} en Ready/Green…"
          while (( SECONDS < END )); do
            read HEALTH STATUS <<<"$(aws elasticbeanstalk describe-environments \
              --region "${AWS_REGION}" \
              --environment-names "${ENV_NAME}" \
              --query 'Environments[0].[Health,Status]' --output text)"
            echo "$(date -u '+%H:%M:%S') HEALTH=${HEALTH} STATUS=${STATUS}"
            if [[ "$STATUS" == "Ready" && "$HEALTH" == "Green" ]]; then
              echo "✅ Environnement prêt."
              exit 0
            fi
            sleep 10
          done
          echo "❌ ${ENV_NAME} pas prêt après ~10 min"
          exit 1

      - name: Déployer sur l’environnement (update-environment)
        shell: bash
        run: |
          set -euo pipefail
          echo "🚀 Mise à jour ${ENV_NAME} vers label: ${{ steps.appver.outputs.label }}"
          aws elasticbeanstalk update-environment \
            --region "${AWS_REGION}" \
            --environment-name "${ENV_NAME}" \
            --version-label "${{ steps.appver.outputs.label }}"

      # ********* C’EST ICI QU’ON ALLONGE LE TIMEOUT GITHUB *********
      - name: Attendre que l’environnement soit GREEN (post-déploiement)
        timeout-minutes: 90
        shell: bash
        env:
          AWS_REGION: ${{ env.AWS_REGION }}
          ENV_NAME:   ${{ env.ENV_NAME }}
        run: |
          set -euo pipefail
          END=$((SECONDS + 90*60))
          echo "⏳ Post-déploiement : on attend le retour en Ready/Green (timeout 90 min)…"
          while (( SECONDS < END )); do
            # AWS renvoie "Green<TAB>Ready" => Health puis Status
            read HEALTH STATUS <<<"$(aws elasticbeanstalk describe-environments \
              --region "${AWS_REGION}" \
              --environment-names "${ENV_NAME}" \
              --query 'Environments[0].[Health,Status]' --output text)"
            echo "$(date -u '+%H:%M:%S') HEALTH=${HEALTH} STATUS=${STATUS}"
            if [[ "$STATUS" == "Ready" && "$HEALTH" == "Green" ]]; then
              echo "✅ Environnement prêt après update."
              exit 0
            fi
            sleep 10
          done

          echo "❌ Timeout  post-déploiement (Ready/Green non atteint)."
          echo "📋 Derniers évènements Elastic Beanstalk (30 min) :"
          aws elasticbeanstalk describe-events \
            --region "${AWS_REGION}" \
            --environment-name "${ENV_NAME}" \
            --start-time "$(date -u -d '30 minutes ago' +%Y-%m-%dT%H:%M:%SZ)" \
            --max-records 200 \
            --query 'Events[].{Time:EventDate,Sev:Severity,Msg:Message}' \
            --output table || true
          exit 1

      - name: Test de fumée (/health) avec retries + fallback HTTP
        shell: bash
        run: |
          set -euo pipefail
          CNAME="$(aws elasticbeanstalk describe-environments \
            --region "${AWS_REGION}" \
            --environment-names "${ENV_NAME}" \
            --query 'Environments[0].CNAME' --output text)"
          if [[ -z "$CNAME" || "$CNAME" == "None" ]]; then
            echo "❌ CNAME introuvable"; exit 1
          fi

          URLS=("https://${CNAME}/health" "http://${CNAME}/health")
          echo "🔎 Smoke test sur: ${URLS[*]}"

          ok="false"
          for url in "${URLS[@]}"; do
            for i in {1..30}; do
              code=$(curl -sk -o /dev/null -w "%{http_code}" "$url" || true)
              echo "Tentative $i → $url → $code"
              if [[ "$code" == "200" ]]; then
                ok="true"
                break
              fi
              sleep $((i<10 ? 3 : (i<20 ? 6 : 10)))
            done
            [[ "$ok" == "true" ]] && break
          done

          if [[ "$ok" != "true" ]]; then
            echo "❌ Smoke test KO. Dump des évènements (15 min) :"
            aws elasticbeanstalk describe-events \
              --region "${AWS_REGION}" \
              --environment-name "${ENV_NAME}" \
              --start-time "$(date -u -d '15 minutes ago' +%Y-%m-%dT%H:%M:%SZ)" \
              --max-records 200 \
              --query 'Events[].{Time:EventDate,Sev:Severity,Msg:Message}' \
              --output table || true
            exit 1
          fi

          echo "✅ Smoke test OK"
