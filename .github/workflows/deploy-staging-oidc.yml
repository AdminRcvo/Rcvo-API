name: Déploiement STAGING via OIDC (Elastic Beanstalk)

on:
  push:
    branches: [ staging ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-staging
  cancel-in-progress: true

env:
  AWS_REGION: eu-west-3
  APP_NAME: Rcvo-backend
  ENV_NAME: Rcvo-Backend-staging
  ROLE_ARN: arn:aws:iam::409818814260:role/GithubOIDCRcvoDeploy

jobs:
  deployer:
    runs-on: ubuntu-latest
    timeout-minutes: 120   # ✅ job étendu (aucun 15 min résiduel)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Vérifier l’identité AWS
        run: aws sts get-caller-identity

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Installer dépendances (lockfile si présent)
        shell: bash
        run: |
          set -euo pipefail
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Build (facultatif)
        run: npm run build --if-present

      - name: Créer l’archive à déployer
        id: pack
        shell: bash
        run: |
          set -euo pipefail
          TS="$(date +%Y%m%d_%H%M%S)"
          SHA="${GITHUB_SHA::7}"
          ZIP="app-${SHA}-${TS}.zip"
          echo "zip=$ZIP" >> "$GITHUB_OUTPUT"
          zip -r "$ZIP" . -x ".git/*" "node_modules/*" "$ZIP"

      - name: Déterminer le bucket EB (CreateStorageLocation idempotent)
        id: bucket
        shell: bash
        run: |
          set -euo pipefail
          BUCKET="$(aws elasticbeanstalk create-storage-location --query S3Bucket --output text)"
          echo "bucket=$BUCKET" >> "$GITHUB_OUTPUT"

      - name: Upload ZIP vers S3
        run: |
          aws s3 cp "./${{ steps.pack.outputs.zip }}" "s3://${{ steps.bucket.outputs.bucket }}/${{ steps.pack.outputs.zip }}"

      - name: Créer/Enregistrer la version d’appli (idempotent)
        id: appver
        shell: bash
        run: |
          set -euo pipefail
          LABEL="${{ steps.pack.outputs.zip }}"
          aws elasticbeanstalk create-application-version \
            --application-name "${APP_NAME}" \
            --version-label "$LABEL" \
            --source-bundle S3Bucket="${{ steps.bucket.outputs.bucket }}",S3Key="${{ steps.pack.outputs.zip }}" \
          || echo "Version déjà existante"
          echo "label=$LABEL" >> "$GITHUB_OUTPUT"

      - name: Attendre READY/GREEN (pré-déploiement)
        shell: bash
        env:
          WAIT_SEC: "10"
          MAX_LOOPS: "60"   # ~10 min
        run: |
          set -euo pipefail
          echo "⏳ Pré-déploiement: ${ENV_NAME} doit être Ready & Green/Ok…"
          for i in $(seq 1 "$MAX_LOOPS"); do
            read HEALTH STATUS <<<"$(aws elasticbeanstalk describe-environments \
              --region "${AWS_REGION}" \
              --environment-names "${ENV_NAME}" \
              --query 'Environments[0].[Health,Status]' --output text)"
            echo "(${i}) HEALTH=${HEALTH}  STATUS=${STATUS}"
            if [[ "$STATUS" == "Ready" && ( "$HEALTH" == "Green" || "$HEALTH" == "Ok" ) ]]; then
              echo "✅ Environnement prêt pour déployer."
              exit 0
            fi
            sleep "$WAIT_SEC"
          done
          echo "❌ ${ENV_NAME} pas prêt avant déploiement."
          exit 1

      - name: Déployer sur l’environnement
        run: |
          aws elasticbeanstalk update-environment \
            --environment-name "${ENV_NAME}" \
            --version-label "${{ steps.appver.outputs.label }}"

      # ====== ICI : NOUVELLE STEP SANS TIMEOUT 15m ======
      - name: Attendre READY/GREEN (post-déploiement) — NO TIMEOUT
        shell: bash
        env:
          READY_WAIT_SEC: "20"
        run: |
          set -euo pipefail
          echo "::group::⏳ Waiter environment-updated (EB)"
          aws elasticbeanstalk wait environment-updated \
            --region "${AWS_REGION}" \
            --environment-names "${ENV_NAME}"
          echo "::endgroup::"

          echo "::group::🔁 Boucle Ready + Green/Ok"
          while : ; do
            read HEALTH STATUS <<<"$(aws elasticbeanstalk describe-environments \
              --region "${AWS_REGION}" \
              --environment-names "${ENV_NAME}" \
              --query 'Environments[0].[Health,Status]' --output text)"
            echo "$(date '+%H:%M:%S')  HEALTH=${HEALTH}  STATUS=${STATUS}"
            if [[ "${STATUS}" == "Ready" && ( "${HEALTH}" == "Green" || "${HEALTH}" == "Ok" ) ]]; then
              echo "✅ Environnement prêt."
              break
            fi
            sleep "${READY_WAIT_SEC}"
          done
          echo "::endgroup::"
      # ================================================

      - name: Test de fumée (/health) avec retries + fallback
        shell: bash
        env:
          RETRIES: "30"
          SLEEP_SEC: "5"
        run: |
          set -euo pipefail
          CNAME="$(aws elasticbeanstalk describe-environments \
            --environment-names "${ENV_NAME}" \
            --query 'Environments[0].CNAME' --output text)"
          echo "CNAME=${CNAME}"
          URLS=("https://${CNAME}/health" "http://${CNAME}/health")
          for URL in "${URLS[@]}"; do
            echo "🌐 Test: $URL"
            for i in $(seq 1 "$RETRIES"); do
              CODE=$(curl -sk -o /dev/null -w "%{http_code}" "$URL" || true)
              echo "  tentative ${i}/${RETRIES} -> ${CODE}"
              if [[ "$CODE" == "200" ]]; then
                echo "✅ Health OK sur ${URL}"
                exit 0
              fi
              sleep "$SLEEP_SEC"
            done
          done
          echo "❌ Health KO sur HTTPS et HTTP."
          exit 1

      - name: (Diag) Derniers événements EB (si échec)
        if: failure()
        shell: bash
        run: |
          set -euo pipefail
          aws elasticbeanstalk describe-events \
            --region "${AWS_REGION}" \
            --environment-name "${ENV_NAME}" \
            --start-time "$(date -u -d '30 minutes ago' +%Y-%m-%dT%H:%M:%SZ)" \
            --max-records 200 \
            --query 'Events[].{Time:EventDate,Sev:Severity,Msg:Message}' \
            --output table || true

      - name: (Diag) Bundle de logs (si échec)
        if: failure()
        shell: bash
        run: |
          set -euo pipefail
          aws elasticbeanstalk request-environment-info \
            --region "${AWS_REGION}" \
            --environment-name "${ENV_NAME}" \
            --info-type tail || true
          sleep 20
          aws elasticbeanstalk retrieve-environment-info \
            --region "${AWS_REGION}" \
            --environment-name "${ENV_NAME}" \
            --info-type tail \
            --query 'EnvironmentInfo[].{When:SampleTimestamp,URL:Message}' \
            --output table || true
